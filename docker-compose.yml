services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - finehelper_mongo:/data/db

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--maxmemory-policy", "noeviction"]
    ports:
      - "6379:6379"

volumes:
  finehelper_mongo:
